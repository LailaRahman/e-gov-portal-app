<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Payment</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="p-5 bg-light">

  <div class="container bg-white p-4 rounded shadow" style="max-width: 500px;">
    <h3 class="mb-4">Confirm Payment</h3>

    <p><strong>Service:</strong> <%= service.name %></p>
    <p><strong>Fee:</strong> $<%= service.fee.toFixed(2) %></p>

    <form action="/citizen/payment/confirm" method="POST">
      <input type="hidden" name="requestId" value="<%= requestId %>">
      <button class="btn btn-success w-100 mt-3">Pay Now</button>
    </form>

    <a href="/citizen/dashboard" class="btn btn-outline-secondary mt-3 w-100">Cancel</a>
  </div>

</body>
</html>
