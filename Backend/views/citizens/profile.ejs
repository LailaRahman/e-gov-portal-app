<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Citizen Profile</title>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />

  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      padding: 40px 15px;
    }
    .profile-card {
      max-width: 600px;
      margin: auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    h2.fw-bold.text-primary {
      text-align: center;
      margin-bottom: 1rem;
    }
    .form-label {
      font-weight: 600;
    }
    .btn-primary {
      font-weight: 600;
    }
    a.back-link {
      display: inline-block;
      margin-top: 20px;
      text-align: center;
      width: 100%;
    }
  </style>
</head>
<body>

  <div class="profile-card">
    <h2 class="fw-bold text-primary">Request Details</h2>

    <% if (success && success.length) { %>
      <div class="alert alert-success"><%= success[0] %></div>
    <% } %>
    <% if (error && error.length) { %>
      <div class="alert alert-danger"><%= error[0] %></div>
    <% } %>

    <form method="POST" action="/citizen/profile" novalidate>
      <div class="mb-3">
        <label for="name" class="form-label">Full Name:</label>
        <input id="name" name="name" type="text" class="form-control" value="<%= user.name %>" required />
      </div>

      <div class="mb-3">
        <label for="national_id" class="form-label">National ID:</label>
        <input id="national_id" name="national_id" type="number" class="form-control" value="<%= user.national_id || '' %>" />
      </div>

      <div class="mb-3">
        <label for="dob" class="form-label">Date of Birth:</label>
        <input id="dob" name="dob" type="date" class="form-control" value="<%= user.dob ? new Date(user.dob).toISOString().split('T')[0] : '' %>" />
      </div>

      <div class="mb-3">
        <label for="contact" class="form-label">Contact Info:</label>
        <input id="contact" name="contact" type="text" class="form-control" value="<%= user.contact || '' %>" />
      </div>

      <button type="submit" class="btn btn-primary w-100">Update Profile</button>
    </form>

    <a href="/citizen/dashboard" class="back-link text-decoration-none text-primary">← Back to Dashboard</a>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
